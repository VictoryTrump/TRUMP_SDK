// src/utils/dataParser.js

const parseData = (data) => {
  // Logic to parse data
  console.log(`Parsing data: ${JSON.stringify(data)}`);
  return data; // Returning parsed data as an example
};

module.exports = { parseData };
